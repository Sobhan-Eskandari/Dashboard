$('.categorySingleDestroy').click(function (event) {    event.preventDefault();    var singleDeleteUrl = this.action;    var dataArray = $(this).serializeArray();    $.ajax({        type: "POST",        url: singleDeleteUrl,        data: dataArray    }).success(function (data) {        $('#postCategories').html(data);        ShowNotification('دسته بندی پاک شد');    }).fail(function () {        ShowNotification('لطفا دوباره تلاش کنید');    });    window.history.pushState("", "", "http://dashboard.dev/posts/create");});$('.tagSingleDestroy').click(function (event) {    event.preventDefault();    var singleDeleteUrl = this.action;    var dataArray = $(this).serializeArray();    $.ajax({        type: "POST",        url: singleDeleteUrl,        data: dataArray    }).success(function (data) {        $('#postTags').html(data);        ShowNotification('برچسب پاک شد');    }).fail(function () {        ShowNotification('لطفا دوباره تلاش کنید');    });    window.history.pushState("", "", "http://dashboard.dev/posts/create");});var createPost = $("#createPostForm");var categoryCheckboxes = createPost.find("input[name=selectedCategories]").val().split(",");var tagCheckboxes = createPost.find("input[name=selectedTags]").val().split(",");// console.log(categoryCheckboxes);// console.log(tagCheckboxes);var selectedTags = {};var selectedCategories = {};$('#postCategories').find('input[type=checkbox]').unbind('click').click(function () {    var checkedId = this.id;    if(this.checked){        categoryCheckboxes.push(checkedId);    }else{        $.each(categoryCheckboxes, function (index, value) {            if(checkedId === value){                categoryCheckboxes.splice(index, 1);            }        });    }    $("#createPostForm").find("input[name=selectedCategories]").val(categoryCheckboxes.toString());});$('#postTags').find('input[type=checkbox]').unbind('click').click(function () {    var checkedId = this.id;    if(this.checked){        tagCheckboxes.push(checkedId);    }else{        $.each(tagCheckboxes, function (index, value) {            if(checkedId === value){                tagCheckboxes.splice(index, 1);            }        });    }    $("#createPostForm").find("input[name=selectedTags]").val(tagCheckboxes.toString());});