/** * Created by Hamid on 25-Jun-17. *//*==========> get the category form after clicking singleDelete button and send an ajax request to CategoryController@destroy <=========*/$('.singleDestroy').click(function (event) {    event.preventDefault();    var singleDeleteUrl = this.action;    var dataArray = $(this).serializeArray();    $.ajax({        type: "POST",        url: singleDeleteUrl,        data: dataArray    }).success(function (data) {        $('#loadFaqs').html(data);        ShowNotification('سوال متداول مورد نظر پاک شد');    }).fail(function () {        ShowNotification('لطفا دوباره تلاش کنید');    });    window.history.pushState("", "", "http://dashboard.dev/faqs");});/*==========> get the category form after clicking singleDelete button and send an ajax request to CategoryController@destroy <=========*//*==========> get the csrf token of multi delete button form <=========*/var token = $('#deleteForm').find('input[name=_token]').val();var csrf = {    name: '_token',    value: token};/*==========> get the csrf token of multi delete button form <=========*//*==========> get the selected categories to be deleted <=========*/var checkboxes = [];var selectedFaqs = {};$('input[type=checkbox]').click(function () {    var checkedId = this.id;    if(this.checked){        checkboxes.push(checkedId);    }else{        $.each(checkboxes, function (index, value) {            if(checkedId === value){                checkboxes.splice(index, 1);            }        });    }    selectedFaqs = {        name: 'ids',        value: checkboxes    };});/*==========> get the selected categories to be deleted <=========*//*==========> send a POST request to delete selected categories using CategoryController@multiDestroy <=========*/$('#multiDestroy').click(function (event) {    event.preventDefault();    $.ajax({        type: "POST",        url: "/faqs-multiDestroy",        data: [            csrf, selectedFaqs        ]    }).success(function (data) {        $('#loadFaqs').html(data);        ShowNotification('سوال (های) متداول پاک شدند');    }).fail(function () {        ShowNotification('یک یا چند سوال متداول انتخاب کنید');    });    window.history.pushState("", "", "http://dashboard.dev/faqs");});/*==========> send a POST request to delete selected categories using CategoryController@multiDestroy <=========*/$('.edit').click(function (event) {    event.preventDefault();    var editCategoryUrl = this.href;    var editForm = $('#editForm');    $.ajax({        type: "GET",        url: editCategoryUrl    }).success(function (data) {        editForm.attr('action', 'http://dashboard.dev/faqs/' + JSON.parse(data).id);        $('#create-div').fadeOut();        $('#edit-div').fadeIn();        editForm.find('input[type=text]').val(JSON.parse(data).question);        CKEDITOR.instances['answerEdit'].setData(JSON.parse(data).answer);        ShowNotification('سوال متداول آماده ویرایش می باشد');    }).fail(function () {        ShowNotification('دوباره تلاش کنید');    });});