/*==========> get the category form after clicking singleDelete button              and send an ajax request to CategoryController@destroy <=========*/$('.singleDestroy').click(function (event) {    event.preventDefault();    var singleDeleteUrl = this.action;    var dataArray = $(this).serializeArray();    $.ajax({        type: "POST",        url: singleDeleteUrl,        data: dataArray    }).success(function (data) {        $('#loadCategories').html(data);        ShowNotification('دسته بندی پاک شد');    }).fail(function () {        ShowNotification('لطفا دوباره تلاش کنید');    });    window.history.pushState("", "", "/categories");});/*==========> get the category form after clicking singleDelete button              and send an ajax request to CategoryController@destroy <=========*//*==========> get the csrf token of multi delete button form <=========*/var token = $('#deleteForm').find('input[name=_token]').val();var csrf = {    name: '_token',    value: token};/*==========> get the csrf token of multi delete button form <=========*//*==========> get the selected categories to be deleted <=========*/var checkboxes = [];var selectedCategories = {};$('input[type=checkbox]').click(function () {    var checkedId = this.id;    if(this.checked){        checkboxes.push(checkedId);    }else{        $.each(checkboxes, function (index, value) {            if(checkedId === value){                checkboxes.splice(index, 1);            }        });    }    selectedCategories = {        name: 'ids',        value: checkboxes    };});/*==========> get the selected categories to be deleted <=========*//*==========> send a POST request to delete selected              categories using CategoryController@multiDestroy <=========*/$('#multiDestroy').click(function (event) {    event.preventDefault();    $.ajax({        type: "POST",        url: "/categories/multiDestroy",        data: [            csrf, selectedCategories        ]    }).success(function (data) {        $('#loadCategories').html(data);        ShowNotification('دسته بندی (ها) پاک شدند');    }).fail(function () {        ShowNotification('یک یا چند دسته بندی انتخاب کنید');    });    window.history.pushState("", "", "/categories");});/*==========> send a POST request to delete selected              categories using CategoryController@multiDestroy <=========*/$('.edit').click(function (event) {    event.preventDefault();    var editCategoryUrl = this.href;    var editForm = $('#editForm');    $.ajax({        type: "GET",        url: editCategoryUrl    }).success(function (data) {        editForm.attr('action', '/categories/' + JSON.parse(data).id);        $('#create-div').fadeOut();        $('#edit-div').fadeIn();        editForm.find('input[type=text]').val(JSON.parse(data).name);        ShowNotification('دسته بندی آماده ویرایش می باشد');    }).fail(function () {        ShowNotification('دوباره تلاش کنید');    });});